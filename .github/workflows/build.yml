name: Build

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Generate manifest
        run: npx esno ./scripts/manifest.ts

      - name: Build background
        run: npx vite build --config vite.config.background.mts

      - name: Build content script
        run: npx vite build --config vite.config.content.mts

      - name: Build web
        run: npx vite build
        continue-on-error: true

      - name: Create release ZIP
        run: |
          cd extension
          zip -r ../tusk-release.zip .

      - name: Upload extension artifact
        uses: actions/upload-artifact@v4
        with:
          name: tusk-extension
          path: extension/
          retention-days: 30

      - name: Upload release ZIP
        uses: actions/upload-artifact@v4
        with:
          name: tusk-release-zip
          path: tusk-release.zip
          retention-days: 30
